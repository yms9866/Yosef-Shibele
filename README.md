This calculator does operations only between two one-digit positive integers. The synax is like for example: 7+2=  
It does not operate between two or more digits
Only one operation at a time(+, -, /, ×, %)
